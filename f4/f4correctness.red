module f4correctness;

asserted procedure correctness_correctness_check(coeffaccum, primetracker, ring, exps, coeffs, coeffs_zz, gens_temp_ff, gens_ff, ht): Boolean;
    begin scalar x;
        return t
    end;

endmodule; % end of module

end; % eof